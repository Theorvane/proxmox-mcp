# LobeHub submission (deferred)

Not published. The repository-owned `lhm.plugin.json` declares the local stdio command and GitHub Release-only distribution. Release manager: insert verified release URL, checksum, version, icon/screenshots, and current schema details. Describe confirmation-required destructive operations.
