# Proxmox MCP OpenClaw wrapper

Deferred metadata only. At publication time this wrapper will download a user-selected, checksum-verified GitHub Release archive and register its local stdio command. It never stores credentials or checks out a mutable branch.
