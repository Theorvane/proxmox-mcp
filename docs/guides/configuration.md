# Configuration

Set `PROXMOX_BASE_URL` to an HTTPS origin, `PROXMOX_TOKEN_ID`, and `PROXMOX_TOKEN_SECRET` in the local MCP client environment. `PROXMOX_TLS_VERIFY` defaults to `true`; setting it to literal `false` emits a stderr warning.
